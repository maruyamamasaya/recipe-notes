# AI 開発ルール

このファイルは、Codex を含む AI エージェントがこのリポジトリで継続的に作業するための恒久的なルールを定める。プロダクト仕様は `docs/PROJECT.md`、技術設計は `docs/ARCHITECTURE.md` を参照し、このファイルを仕様書として肥大化させないこと。

## 基本原則

- 実装前に既存コード、ディレクトリ構造、依存関係、関連ドキュメント、テストを確認する。
- 既存の設計意図と規約を尊重し、変更範囲を目的達成に必要な最小限にする。
- 無関係なリファクタリングを同じ変更に含めない。
- 車輪の再発明を避け、フレームワークや言語の公式機能で解決できるものを不要に独自実装しない。
- 新しい依存関係は、既存機能や標準機能で代替できないか、保守性と安全性に問題がないかを確認してから追加する。
- 同じロジックを複数箇所に重複実装せず、既存の共通処理を探して再利用する。
- 「将来必要になるかもしれない」という理由だけで抽象化や拡張点を増やさず、現在の要件に沿って設計する。
- 仕様や設計が不明な場合は推測で確定せず、`TBD` として明示するか人間に確認する。
- 設計や仕様を変更した場合は、責務に対応する `docs/` 内の文書と Decision Log も必要に応じて更新する。

## 実装フロー

原則として、次の順番で進める。

1. 調査
2. 影響範囲の確認
3. 実装
4. lint
5. typecheck
6. test
7. 必要に応じて build
8. 結果報告

利用可能なコマンドは `docs/DEVELOPMENT.md` とプロジェクト設定を確認する。コマンドが未定義なら架空のコマンドを実行・記載せず、その事実を報告する。変更は小さく分け、各段階で検証可能な状態を保つ。

## 品質

- エラーを握り潰さず、利用者または運用者が原因を追跡できる形で処理する。
- テストを通すためだけの不自然な分岐、ハードコード、実運用と異なる実装を行わない。
- 型安全性を可能な限り維持し、根拠のない型アサーションや型検査の無効化を避ける。
- lint、typecheck、test が失敗した場合は原因を確認し、変更との関係を明示する。
- 既存テストを壊さない。仕様変更に伴う期待値変更は、その理由を明確にする。
- 新しい重要ロジックや不具合修正には、必要に応じて回帰テストを追加する。

## セキュリティ

- API Key、Password、Token、秘密鍵などの秘密情報をコードや文書へ直接記述しない。
- `.env` など秘密情報を含むファイルを Git にコミットしない。共有が必要な変数名は値を含まないサンプルで管理する。
- 認証・認可を UI の表示制御だけに依存せず、API やデータベース側でもアクセス制御を実施する。
- ユーザー入力を信用せず、境界で検証・正規化し、出力先に応じた安全対策を行う。
- ログやエラーメッセージへ秘密情報や不要な個人情報を出力しない。

## データベース

- 破壊的変更を安易に行わず、Schema 変更前に既存データ、API、運用への影響を確認する。
- Schema の変更は原則として再現可能な migration で管理する。
- データモデルごとの責務、所有者、整合性の境界を明確にする。
- データを必要以上に JSON などへ押し込まず、検索、更新、制約、整合性を考えて設計する。
- migration の適用順序、ロールバックまたは復旧方法を検討し、`docs/DATABASE.md` を更新する。

## 大きな変更

次に該当する変更は、実装前に理由、影響範囲、代替案、移行・復旧方法を整理し、必要な合意を得る。採用した判断は `docs/DECISIONS.md` に記録する。

- Architecture の変更
- Database Schema の大幅な変更
- 認証方式の変更
- 大規模リファクタリング
- 主要ライブラリの追加・変更
- 外部サービスの追加
- 既存仕様との互換性を壊す変更

## 完了報告

作業完了時は、最低限、次を簡潔に報告する。

- 何を変更したか
- なぜ変更したか
- 何を検証したか
- lint、typecheck、test、build の結果（未実施の場合は理由）
- 残課題
- 人間による確認が必要な事項
